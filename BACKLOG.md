# Backlog

This file is **generated**. The active sprint section between the marker pair below is rewritten by `npm run backlog:render` from `backlog/sprints/*.sprint.json`. Do not edit the content between the markers — the next render will overwrite it. Everything outside the markers (this preamble, the Next-Sprint placeholder at the bottom) stays as hand-authored Markdown.

<!-- backlog:render:start -->

## Current Sprint: S098 — QA followups (S90/S96 verification) + entity create/delete probes + input inject + game-design dogfood

Status: **active** (started 2026-05-20). Source: `backlog/sprints/S098.sprint.json`.

### Stories

- **AGF-PROBE-ENTITY-CREATE** — POST /__agf/entity creates a new entity with an initial component map _(implemented)_
  runtime.createEntity(entityId, components) calls world.addEntity + iterative world.setComponent for each [name, value] pair; returns { kind: 'ok', components } on success or { kind: 'entity-exists' } when the id is taken. The dev-bridge POST /entity route runs the new exported pure helper `validateEntityCreateBody` (rejects non-object body, empty/non-string entityId, missing/non-object/array components with 400 AGF_BRIDGE_INVALID_ENTITY_CREATE), forwards via the new page-bridge `entity-create` RPC, and maps `entity-exists` to 409 AGF_PROBE_ENTITY_EXISTS. Plumbed end-to-end through src/app.ts + window.__agf.createEntity.
- **AGF-PROBE-ENTITY-DELETE** — DELETE /__agf/entity/<entityId> removes an entity wholesale _(implemented)_
  Completes the entity CRUD triplet (create / read / delete + S097's component write). runtime.deleteEntity(entityId) calls world.removeEntity and returns { kind: 'ok' } | { kind: 'entity-not-found' }. The dev-bridge DELETE /entity/:entityId route reuses parseEntityPath (S097), forwards via the new page-bridge `entity-delete` RPC, maps entity-not-found to 404. Required widening the method gate from {GET, POST} to {GET, POST, DELETE}. Plumbed through src/app.ts + window.__agf.deleteEntity.
- **AGF-PROBE-INPUT-INJECT** — POST /__agf/input/action lets QA fire game inputs without DOM focus (QA-006) _(implemented)_
  Promoted from QA-2026-05-20-006. Project-agnostic input-injection surface: engine ships a generic `InputAction { action, value? }` transient component (schema in schemas/components/gamefeel.schema.json, referenced from scene.schema.json). runtime.injectInput(entityId, action, value?) writes the transient + returns { kind: 'ok' | 'entity-not-found' }. Project input systems read + clear it each frame, same way they translate keyboard events into project-specific transients. examples/kaboom-crew/src/systems/player-input-system.ts grows the consumer path: 'place-bomb' → PlaceBombRequest, 'restart' → RoundRestartRequest, 'move-up/down/left/right' → GridMover.queuedDirection vector, 'stop' → {0,0}. Unknown action names are silently consumed (engine remains project-agnostic; projects can grow new verbs without engine changes). Dev-bridge POST /input/action runs the new exported `validateInputInjectBody`; missing/empty entityId or action returns 400 AGF_BRIDGE_INVALID_INPUT_ACTION; entity-not-found maps to 404.
- **BUG-ENGINE-CLI-INSPECT-SUMMARY-FLAKE** — engine-cli 'prints a stable inspect summary' test flakes at 5000ms (QA-004) _(implemented)_
  Promoted from QA-2026-05-20-004. Each `it()` in tests/unit/engine-cli.test.ts spawns a fresh `tsx engine/tools/cli.ts` — cold-start of tsx + the full CLI dep graph reliably blows the 5000ms vitest default on slower workers. Fix: per-test timeout override `CLI_TEST_TIMEOUT_MS = 15000` applied to every CLI integration case. Pre-warm was considered but rejected — each `spawn` is a fresh process so Node's module cache doesn't survive across spawns; a beforeAll warmup costs cycles without speeding the individual cases. Test-infra fix only; no product change.
- **REGRESSION-ENGINE-CLI-INSPECT-SUMMARY** — Lock the engine-cli flake fix with a deterministic timing test (QA-005) _(implemented)_
  Promoted from QA-2026-05-20-005 (regressionFor QA-004). Adds 'S98 regression: 3x warm inspect runs finish under 10s each' — loops 3 inspect runs back-to-back, measures wall-clock per run, asserts each < 10000ms. Per-test timeout is CLI_TEST_TIMEOUT_MS * 3 = 45000ms to cover the full sequence. Catches future regressions on either axis: a stuck process (>10s wall) or a slow-but-finishing process (still <10s each but cumulative >45s).
  Depends on: BUG-ENGINE-CLI-INSPECT-SUMMARY-FLAKE.
- **GAME-DESIGN-DOGFOOD** — First end-to-end dogfood of the propose:new → propose:promote flow _(implemented)_
  First real end-to-end run of the S097 toolchain. (1) `node scripts/backlog/propose.mjs new` scaffolded GDP-2026-05-20-001 (decal-on-grid renderer, kind=feature, priority=should, epic=M27-DECAL-LAYER) and GDP-2026-05-20-002 (region-rule modifier, kind=mechanic, priority=could, epic=M28-REGION-RULES). (2) Both files filled in by hand from notes/dynabomber-readiness-analysis.md §14 + §15. (3) `npm run backlog:check` validated both proposals — clean. (4) S099.sprint.json (pending) seeded as the promotion target. (5) `node scripts/backlog/promote-proposed.mjs --into S099 --skip-check` promoted both proposals: source files moved under backlog/proposed-stories/archive/S099/, S099 sprint JSON gained two pending FEAT-* stories with the rationale + acceptance hints embedded in the summary. **Observations from the dogfood (worth a follow-up story):** (a) The promote-proposed slug-truncation at 24 chars produced ugly ids like `FEAT-DECAL-ON-GRID-RENDERER-P-001` (the 'P' is the start of 'PRIMITIVE' before truncation). Function works but readability suffers — slug length should probably grow OR truncate at a word boundary. (b) The 1-second NNN allocator caps at 999 — fine for the foreseeable future but worth a known-limit note. (c) propose:new prints `[propose] created /abs/path/...` to stderr AND the path to stdout, which is useful for piping into $EDITOR. The dogfood ran in one terminal so this was fine; a CI-style automation would just consume stdout.
- **DOC-QA-INVALID-TICKET-FLOW** — docs/qa/invalid-ticket-handling.md — how dev rejects QA tickets that don't apply _(implemented)_
  docs/qa/invalid-ticket-handling.md documents the convention used ad-hoc this sprint when rejecting QA-001 + QA-003. Four common rejection cases (already-fixed, intentional behavior, duplicate, out-of-scope) each get a row in a table with examples + a uniform mechanic: `git mv` to backlog/qa-tickets/archive/rejected/, append a bullet to that directory's README.md with severity / type / category / rationale + commit/sprint references. Also covers the QA-side: read the rationale, don't refile (PR-comment if you disagree, file a fresh ticket with new context if there's more to say). Cross-links to design.md, agent.md, regression-promotion.md, and the live rejected/README.md ledger. The rejected README itself got a back-link to the new doc.
- **KABOOM-BLAST-DANGER-DECAL** — Danger overlay on cells inside a bomb's projected blast radius _(implemented)_
  examples/kaboom-crew/src/systems/danger-decal-system.ts spawns a translucent red box on every cell inside a bomb's projected blast while the fuse is below the warning threshold (default 1 s; configurable via `warningFuseSeconds` option). Stable entity ids per cell (`danger.<gx>.<gz>`) so the system can `hasEntity`-check rather than carry a side map. Each fixedUpdate computes the should-be-on cell set, spawns missing DangerTiles, removes DangerTiles whose cell is no longer threatened. Hard-block GridOccupants stop the fan (same rule as the actual blast propagation). Project-local `DangerTile` component is a pure marker (no fields) registered in the project's scene-extensions schema. Uses standard Transform + MeshRenderer + GridPosition primitives — no renderer changes, no decal layer (M27 still planned).

<!-- backlog:render:end -->

## Next Sprint (placeholder)

After S78 lands the backlog engine, the next sprint is the DynaBomber pre-game platform: `BACKLOG-NEXT` + `BACKLOG-CLI-MUTATE` from this sprint's follow-ups, then `DYN-ortho-camera` / `DYN-damped-follow` / `DYN-2d-hud-runtime` / `DYN-grid-primitives` from `notes/dynabomber-readiness-analysis.md` §11.
