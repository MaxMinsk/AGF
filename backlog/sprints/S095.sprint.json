{
  "agfFormatVersion": 1,
  "id": "S095",
  "title": "Polish — camera shake / spawn pop + agent free-cam + audio dial + snapshot history",
  "status": "active",
  "startedAt": "2026-05-20",
  "stories": [
    {
      "id": "KABOOM-CAMERA-EASING-ADOPT",
      "title": "Kaboom Crew adopts S091 named easings — death tip + camera shake decay",
      "status": "implemented",
      "summary": "death-animation-system.ts now imports `easingCurves` from engine/core/systems/tween-system.ts; `deathFallPitch()` samples `easingCurves.easeOutBack(t)` instead of the previous inline `1 - (1 - t) * (1 - t)` — dying bombers tip past 90° (~110% of target) and rock back to settle. camera-shake-system.ts dropped the `decayPerSecond` knob in favor of `durationSeconds` (default 0.45 s); a new pure helper `cameraShakeEnvelope(elapsed, duration)` returns `1 - easingCurves.easeOutElastic(t)`, which opens at 1 and dips below zero mid-curve before settling at 0 — gives the shake a small bouncy stop instead of monotonic exponential fade. The system tracks `peakIntensity` + `elapsed` per shake window; a fresh BlastEvent resets `elapsed` so the bounce stays in sync with the latest detonation.",
      "verification": [
        "acceptance: A dying bomber visibly tips past 90° and settles back (theatrical overshoot, not a smooth monotonic glide). Camera shake on a nearby blast stops with a tiny oscillation rather than fading evenly.",
        "examples/kaboom-crew/src/systems/death-animation-system.ts: easingCurves.easeOutBack drives deathFallPitch",
        "examples/kaboom-crew/src/systems/camera-shake-system.ts: cameraShakeEnvelope exported; shake intensity sampled from 1 - easingCurves.easeOutElastic over the active shake window",
        "+2 unit tests on deathFallPitch: peak sample exceeds Math.PI/2 by <15%, pre-peak segment is monotonic, endpoint lands at Math.PI/2",
        "+3 unit tests on cameraShakeEnvelope: endpoints 0/1 exact, defensive duration<=0 returns 0, mid-curve dips below 0 (bounce signature)",
        "Existing camera-shake tests updated to track durationSeconds option (decayPerSecond removed)"
      ],
      "epic": "KABOOM-CREW-MVP-1"
    },
    {
      "id": "KABOOM-SPAWN-POP-TWEEN",
      "title": "Bomb / pickup mesh scale 0 → 1 with easeOutBack on spawn",
      "status": "implemented",
      "summary": "bomb-placement-system.ts + pickup-spawn-system.ts now stamp `Transform.scale = [0, 0, 0]` on the freshly-spawned entity and attach a `Tweens` component driving scale to the final size over 200 ms with `easeOutBack`. The engine's TweenSystem (registered by start.ts) advances each tween in fixedUpdate and removes the Tween component on completion — no per-project advance code needed. Final scales: bombs `[0.35, 0.35, 0.35]`, pickups inherit the per-kind PICKUP_VISUAL.scale.",
      "verification": [
        "acceptance: New bombs visibly pop into existence (scale jumps from 0 to slightly past 1 then settles to full); pickups dropped on a softblock-destroyed cell do the same — no longer flash in at full size.",
        "bomb-placement-system.ts: stamps Tweens[{component:'Transform', property:'scale', from:[0,0,0], to:[0.35,0.35,0.35], duration:0.2, ease:'easeOutBack'}]",
        "pickup-spawn-system.ts: same shape, `to` matches per-kind visual.scale",
        "+2 unit tests verifying the Tween component shape on both spawn paths"
      ],
      "epic": "KABOOM-CREW-MVP-1"
    },
    {
      "id": "AGF-RENDER-DEBUG-FREECAM",
      "title": "Agent-driven free-fly camera for debugging — POST /__agf/render/freecam",
      "status": "pending",
      "summary": "Add an opt-in free-fly camera the agent can position arbitrarily without touching the game's camera control. Surface: `runtime.renderer.setFreeCam({ position, lookAt } | null)` — when non-null, the renderer creates (and pins active) a debug PerspectiveCamera at the given pose; when null, the project's normal active camera takes over again. Dev-bridge: `POST /__agf/render/freecam { position, lookAt }` + `POST /__agf/render/freecam { off: true }` + `GET /__agf/render/freecam`. Useful for inspecting blast aftermath, off-screen entities, prefab layout. Pure agent observability, not a player feature.",
      "verification": [
        "acceptance: POST /__agf/render/freecam { position: [10, 8, 10], lookAt: [0, 0, 0] } moves the rendered view to that pose immediately; POST { off: true } restores the previous active camera and the player can keep playing.",
        "engine/render/three-render-adapter.ts: setFreeCam() owns a private PerspectiveCamera; `pickAtNdc` and CameraSyncSystem must observe the override",
        "Dev-bridge route gets a row in docs/agent-probes.md",
        "+ unit test on a fake adapter scene verifying that setFreeCam swaps the active camera and clearing it restores the prior one"
      ],
      "epic": "AGF-ENGINE-OBSERVABILITY"
    },
    {
      "id": "AGF-AUDIO-MASTER-VOLUME",
      "title": "AudioBus grows a master volume dial — POST /__agf/audio/master-volume",
      "status": "implemented",
      "summary": "engine/runtime/audio/audio-bus.ts grew `setMasterVolume(value): number` + `getMasterVolume(): number`. Master multiplies per-call volume in `play()`: `el.volume = clamp(master * (volume ?? 1), 0, 1)`. Non-finite inputs to setMasterVolume are ignored. Plumbed through src/app.ts (getAudioMasterVolume / setAudioMasterVolume), window.__agf, and GET/POST /__agf/audio/master-volume on the dev bridge (AGF_BRIDGE_INVALID_AUDIO_VOLUME on non-number).",
      "verification": [
        "acceptance: POST /__agf/audio/master-volume { value: 0 } silences subsequent plays; POST { value: 1 } restores; per-play `volume: 0.5` plays at 50% of master.",
        "engine/runtime/audio/audio-bus.ts: setMasterVolume / getMasterVolume on the AudioBus interface; play() applies master * per-call",
        "+3 unit tests in tests/unit/audio-bus.test.ts: getMasterVolume default 1 + clamp + non-finite ignored; master * per-call multiplication; final clamp at 1.0 against excessive per-call volume"
      ],
      "epic": "AUDIO"
    },
    {
      "id": "KABOOM-AUDIO-MIXER-DUCK-ON-MATCH-END",
      "title": "Briefly duck SFX while a match-* chime plays",
      "status": "pending",
      "summary": "Build on AGF-AUDIO-MASTER-VOLUME. When the audio-binding-system fires `match-won/-lost/-draw`, it asks the audio-fx to schedule a 600 ms duck: master gain dips to 0.3 immediately, ramps back to 1.0 over the duck window. Implemented inside Kaboom's audio-fx.ts (it owns the procedural-synth path); positional bomber events that fire during the duck inherit the lower gain. Uses requestAnimationFrame or AudioParam ramps, no scheduler change.",
      "verification": [
        "acceptance: When a match ends, the win/lose chime is clearly audible and any bomb / footstep events that happen during the chime feel quieter than usual; volume restores afterwards without a pop.",
        "examples/kaboom-crew/src/audio-fx.ts: `duckFor(ms, depth)` helper + match-* events trigger it",
        "+ unit test on the audio-fx fake context: after triggering match-won, masterGain.gain has setValueAtTime(0.3, now) followed by linearRampToValueAtTime(1, now + 0.6)"
      ],
      "epic": "KABOOM-CREW-MVP-1",
      "dependsOn": ["AGF-AUDIO-MASTER-VOLUME"]
    },
    {
      "id": "AGF-PROBE-SNAPSHOT-HISTORY",
      "title": "Snapshot ring buffer — GET /__agf/snapshot?at=-N replays history",
      "status": "pending",
      "summary": "The current /__agf/snapshot probe returns only the live snapshot. Keep a fixed-size ring buffer (default 32) of past snapshots so an agent can diff against history (e.g. `snapshot?at=-1` vs live to see what changed last frame). New route param `at` accepts a non-positive integer index; -0 (or omitted) returns live, -1 returns the previous, etc. `at` beyond the buffer returns 400 AGF_PROBE_SNAPSHOT_OUT_OF_RANGE. The ring is filled cheaply — one entry per fixedUpdate.",
      "verification": [
        "acceptance: GET /__agf/snapshot returns the live snapshot; GET /__agf/snapshot?at=-1 returns the previous fixedUpdate's snapshot; both are deeply different on a tick where a known component changed.",
        "engine/runtime/start.ts: ring buffer + indexed lookup; engine/dev/page-bridge.ts: snapshot kind accepts `{ at }` payload",
        "+ unit test ringing 5 frames and verifying at=-0/-1/-2/-3 each return the right historical snapshot; at=-99 returns the AGF_PROBE_SNAPSHOT_OUT_OF_RANGE error"
      ],
      "epic": "AGF-ENGINE-OBSERVABILITY"
    },
    {
      "id": "DOC-AGENT-PROBES-REFRESH",
      "title": "Refresh docs/agent-probes.md — freecam, audio master, snapshot history",
      "status": "pending",
      "summary": "Pure docs sweep. Add rows for `/__agf/render/freecam`, `/__agf/audio/master-volume`, and the `at` query param on `/__agf/snapshot`. Include curl recipes + the error code for each. Verify every probe documented in the file still matches its handler signature (catch drift).",
      "verification": [
        "acceptance: docs/agent-probes.md table-of-contents lists all three new routes; the section for each contains a working curl recipe.",
        "Each new section calls out its error code (AGF_PROBE_SNAPSHOT_OUT_OF_RANGE, AGF_BRIDGE_INVALID_AUDIO_VOLUME, AGF_BRIDGE_INVALID_FREECAM)"
      ],
      "epic": "AGF-ENGINE-OBSERVABILITY"
    }
  ]
}
