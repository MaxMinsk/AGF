{
  "agfFormatVersion": 1,
  "id": "S090",
  "title": "Engine time-scale primitive + Kaboom Crew feel pass",
  "status": "active",
  "startedAt": "2026-05-20",
  "stories": [
    {
      "id": "AGF-RUNTIME-TIME-SCALE",
      "title": "runtime.setTimeScale(x) — engine-level slow-mo / fast-forward",
      "status": "implemented",
      "summary": "RuntimeHandle gains `setTimeScale(scale)` + `getTimeScale()`. The loop multiplies the real wallclock dt by the scale before advanceFixedStep / setting time.dt — so fixed-step accumulator and frame dt scale identically (deterministic systems still tick on the same simulated clock, just at a different tempo). Exported `clampTimeScale(value)` enforces [0.05, 4]; non-finite inputs (NaN, ±Inf) fall back to 1.",
      "verification": [
        "+4 unit tests under tests/unit/runtime-time-scale.test.ts cover identity range, low-clamp, high-clamp, non-finite fallback",
        "engine/runtime/start.ts: `timeScale` variable defaults to 1; `setTimeScale` clamps + assigns + returns; tick() reads `wallDt * timeScale`",
        "RuntimeHandle TypeScript surface gains setTimeScale + getTimeScale with TSDoc"
      ],
      "epic": "AGF-ENGINE-OBSERVABILITY"
    },
    {
      "id": "AGF-DEV-BRIDGE-TIME-SCALE",
      "title": "POST /__agf/runtime/timescale over the dev bridge",
      "status": "implemented",
      "summary": "Mirror the in-page accessor over the dev bridge so scripted playtests can flip time-scale without poking at window.__agf. `POST /__agf/runtime/timescale { value: 0.25 }` proxies to runtime.setTimeScale and returns the clamped scale; `GET /__agf/runtime/timescale` returns the live value. Plumbed through agf-dev-bridge route table + page-bridge channels + main.ts handler.",
      "verification": [
        "agf-dev-bridge.ts: GET /runtime/timescale → 'runtime-timescale' channel; POST → 'runtime-timescale-set' with `value` body",
        "page-bridge.ts: API surface gains getTimeScale + setTimeScale; channel handlers return `{ scale }`",
        "src/app.ts + src/main.ts forward to runtime.getTimeScale / setTimeScale",
        "docs/agent-probes.md adds a /__agf/runtime/timescale section"
      ],
      "epic": "AGF-ENGINE-OBSERVABILITY"
    },
    {
      "id": "KABOOM-BOMB-FUSE-WIGGLE",
      "title": "Bomb mesh wiggles in scale as fuse nears 0",
      "status": "pending",
      "summary": "S87 added a color pulse on bomb fuse, but the mesh sits still otherwise. Add a per-frame Transform.scale modulation in BombFuseSystem: `1 + 0.12 * Math.sin(t * pulseFreq)` where pulseFreq grows as fuseRemaining drops. The scale ratchets back to baseline when fuseRemaining > 2 (no wiggle on a freshly-placed bomb). Pure-helper `bombWiggleScale(fuseRemaining)` exported so the unit test can lock the curve.",
      "verification": [
        "bombWiggleScale(2.5) returns ~1.0 (effectively still); bombWiggleScale(0.2) oscillates with high amplitude + high frequency",
        "Bomb mesh Transform.scale is rewritten each fixed step when fuseRemaining < 2",
        "+1 unit test covers the helper's shape"
      ],
      "epic": "KABOOM-CREW-MVP-1"
    },
    {
      "id": "KABOOM-DEATH-FALL",
      "title": "Dying bomber visibly tips over before despawn",
      "status": "pending",
      "summary": "Today death is instant — alive flips false, sprite goes to half-opacity / vanishes (whichever the renderer does), particle puff fires. Add a short death animation: when BomberStats.alive flips true → false, set Transform.rotation to a fall angle (90° on X axis), and pause the GridMover for 0.5 s before death finalisation. Reads better; emphasises agency.",
      "verification": [
        "When alive: false, bomber Transform.rotation[0] crosses 90° within ~0.3 s",
        "GridMover.queuedDirection is zeroed on death (no zombie movement)",
        "+1 unit test exercising the rotation lerp"
      ],
      "epic": "KABOOM-CREW-MVP-1"
    },
    {
      "id": "KABOOM-FOOTSTEP-TICK",
      "title": "Tiny click on every grid-cell crossing",
      "status": "pending",
      "summary": "Player has no audio feedback for movement — only blast/bomb/pickup/death. Add a `footstep` AudioEventKind that fires when a bomber crosses a grid cell (GridPosition gx/gz changes). audio-binding tracks per-bomber positions across ticks. audio-fx plays a 25 ms muted click — barely audible solo, satisfying when chained. Inherits the mute toggle.",
      "verification": [
        "audio-binding-system emits 'footstep' on GridPosition change for every bomber",
        "audio-fx.ts adds a short low-gain click",
        "+1 unit test covers the GridPosition-diff detection"
      ],
      "epic": "KABOOM-CREW-MVP-1"
    },
    {
      "id": "KABOOM-MINIMAP-DANGER-OVERLAY",
      "title": "Minimap tints projected blast cells red",
      "status": "pending",
      "summary": "The minimap shows bombs as dots, but a player can't see the danger zone from a glance. Compute the same projected-blast cells the bot AI already uses (bomb origin + cardinals × range, blocked by hard walls) and overlay them as semi-transparent red rectangles before the bomb dot is drawn. Same data source as bot-ai's buildDangerMap — extracted into a tiny shared helper.",
      "verification": [
        "Helper `projectedBlastCells(world, occupancy)` returns the union of every live bomb's cardinal blast cells (S82 propagation semantics)",
        "Minimap renders one red 4px square per cell at 40% alpha under the existing markers",
        "+1 unit test exercising the helper against a fixture with one bomb + a hard wall"
      ],
      "epic": "KABOOM-CREW-MVP-1"
    },
    {
      "id": "AGF-DOCS-RUNTIME-HANDLE-AUDIT",
      "title": "Inline JSDoc audit on RuntimeHandle surface",
      "status": "pending",
      "summary": "RuntimeHandle has grown a lot: invalidateAsset, snapshot, applyCommands, save/load, pick, hud, audio, frameTiming, startRecording/stopRecording, setDebugSystem (S89), setTimeScale (this sprint). Sweep `engine/runtime/start.ts` so every method on the exported type has a one-paragraph TSDoc comment explaining the contract + the sprint that introduced it. No behaviour changes.",
      "verification": [
        "Every method on RuntimeHandle has a TSDoc block; lint passes",
        "git grep for 'RuntimeHandle' in docs lines up with the implemented surface"
      ],
      "epic": "AGF-ENGINE-OBSERVABILITY"
    }
  ]
}
