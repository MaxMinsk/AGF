{
  "agfFormatVersion": 1,
  "id": "S083",
  "title": "Engine observability — logging policy + CI gate + motion smoothness",
  "status": "active",
  "startedAt": "2026-05-18",
  "stories": [
    {
      "id": "AGF-MOTION-SMOOTHNESS-PROBE",
      "title": "Motion smoothness — frame-time probe + GridMover stutter fix",
      "status": "pending",
      "summary": "Build a deterministic Playwright probe that measures frame-delta jitter while the player walks across the Kaboom Crew arena. Capture a histogram of frame durations + per-cell tween dt deltas. Investigate whether GridMover's cell-tween + lane-assist introduce visible micro-stutters (cell boundary snaps, recompute order, BatchingSystem invalidation churn). Land a targeted fix if root cause is in the renderer/scheduler/tween path; if it turns out to be macOS PSU/throttling, document the workaround.",
      "verification": [
        "tests/e2e/motion-smoothness.spec.ts captures ≥3 s of frame-time samples while gotoCell drives the player across ≥5 cells, asserts P95 frame-delta ≤ N ms on the smoke project",
        "before/after metrics in the PR body (median frame ms, P95 frame ms, max stall ms)"
      ],
      "epic": "AGF-ENGINE-OBSERVABILITY"
    },
    {
      "id": "AGF-LOG-POLICY-DOC",
      "title": "Logging policy doc — severity scale + diagnostics-vs-console rules",
      "status": "pending",
      "summary": "Write `docs/diagnostics-policy.md` capturing the severity scale (fatal/error/warn/info/debug/trace), when-to-use-diagnostics-vs-console, the structured-payload schema (domain_verb code + JSON context), and the rules for engine vs example code. Source: notes/logging-policy-thinking.md.",
      "verification": [
        "docs/diagnostics-policy.md exists; referenced from CLAUDE.md + AGENTS.md"
      ],
      "epic": "AGF-ENGINE-OBSERVABILITY"
    },
    {
      "id": "AGF-LOG-CI-GATE",
      "title": "CI gate: ban raw console.log in engine/** + examples/**/src/**",
      "status": "pending",
      "summary": "scripts/check-no-console-log.mjs walks the configured globs and rejects bare console.{log,warn,error}. Allowlist via `// agf-allow:console <reason>` markers. Wire into `npm run repo:hygiene` (already part of preflight) so CI fails when someone slips a log in.",
      "verification": [
        "script flags an introduced console.log; passes on the current tree after audit",
        "preflight runs the gate before typecheck"
      ],
      "dependsOn": ["AGF-LOG-POLICY-DOC"],
      "epic": "AGF-ENGINE-OBSERVABILITY"
    },
    {
      "id": "AGF-LOG-AUDIT-ENGINE",
      "title": "Audit + purge existing console.* in engine/** under the new policy",
      "status": "pending",
      "summary": "Walk every `console.*` call site inside engine/** and either route it through the diagnostics channel, downgrade to debug-flag gated, or add an `agf-allow:` marker with a one-line rationale. Goal: a clean baseline before the CI gate flips on.",
      "verification": [
        "check-no-console-log.mjs passes against engine/** with zero unmarked sites",
        "PR diff lists every conversion"
      ],
      "dependsOn": ["AGF-LOG-CI-GATE"],
      "epic": "AGF-ENGINE-OBSERVABILITY"
    },
    {
      "id": "AGF-LOG-PER-SYSTEM-DEBUG",
      "title": "Named per-system debug loggers + AGF_DEBUG flag",
      "status": "pending",
      "summary": "Engine adds a `createDebugLogger(name)` factory that returns a logger gated by a project-wide `AGF_DEBUG` env / query-param. Systems opt-in (`const log = createDebugLogger('grid-movement')`) so debug output can be filtered by name and stays silent by default.",
      "verification": [
        "unit test: logger is no-op when AGF_DEBUG is unset; emits when set to '*' or matching name",
        "at least one engine system (grid-movement) consumes the new logger"
      ],
      "dependsOn": ["AGF-LOG-POLICY-DOC"],
      "epic": "AGF-ENGINE-OBSERVABILITY"
    },
    {
      "id": "AGF-LOG-LIFECYCLE-TRACES",
      "title": "Structured lifecycle traces — scene load / system register / runtime stop",
      "status": "pending",
      "summary": "Emit one structured trace per lifecycle event (scene load start/end with entity counts, system register/unregister, runtime stop) through the existing diagnostics channel. No console.log path. Helps post-mortems on hangs + ghost-entity bugs.",
      "verification": [
        "diagnostics buffer captures `agf.scene.loaded`, `agf.system.registered`, `agf.runtime.stopped` payloads in an integration test"
      ],
      "dependsOn": ["AGF-LOG-POLICY-DOC"],
      "epic": "AGF-ENGINE-OBSERVABILITY"
    },
    {
      "id": "AGF-LOG-DOCTOR-DIAGNOSTICS",
      "title": "engine doctor reports diagnostics noise summary",
      "status": "pending",
      "summary": "Add a `diagnostics` section to `engine doctor` that summarises total events by severity + top-N codes since boot. Helps spot a system that's spam-warning every frame without grep-ing console.",
      "verification": [
        "engine doctor --json prints a `diagnostics: { total, bySeverity, topCodes }` block on a sample project"
      ],
      "dependsOn": ["AGF-LOG-LIFECYCLE-TRACES"],
      "epic": "AGF-ENGINE-OBSERVABILITY"
    },
    {
      "id": "AGF-LOG-BOOT-NOISE-BUDGET",
      "title": "Boot noise budget — fail when startup emits more than N logs",
      "status": "pending",
      "summary": "Preflight smoke test asserts that booting hello-3d emits ≤ N (small, e.g. 5) info-level diagnostics before idle. Catches regressions where someone adds a per-frame log to a startup path.",
      "verification": [
        "smoke e2e fails when a deliberate noisy log is added; passes on the cleaned tree"
      ],
      "dependsOn": ["AGF-LOG-AUDIT-ENGINE"],
      "epic": "AGF-ENGINE-OBSERVABILITY"
    },
    {
      "id": "AGF-LOG-ENTITY-RECREATED",
      "title": "Detect + log entity recreation regressions",
      "status": "pending",
      "summary": "When `entity.create` lands an id that was deleted earlier in the same session, emit a `agf.entity.recreated` warning with both call stacks (when stack traces are cheap). Root-cause aid for ghost-entity bugs like the S082 restart issue.",
      "verification": [
        "unit test: recreating a previously-deleted id triggers the warning; same-frame ids don't"
      ],
      "dependsOn": ["AGF-LOG-LIFECYCLE-TRACES"],
      "epic": "AGF-ENGINE-OBSERVABILITY"
    },
    {
      "id": "AGF-AGENT-RENDERER-PROBE",
      "title": "Engine renderer probe — `runtime.renderer.inspect()` + CLI",
      "status": "pending",
      "summary": "First-class engine tool: `runtime.renderer.inspect()` returns a compact JSON dump of the Three.js scene tree (Mesh/InstancedMesh/Light counts + visibility), BatchingSystem bucket records (key, capacity, live, lastWorld memo) and MeshHandleRegistry entries. `engine probe <projectDir>` exposes the same via the CLI. The ad-hoc probe used during the S082 restart bug becomes a one-curl primitive.",
      "verification": [
        "unit test: inspect() output schema validates against schemas/renderer-inspect.schema.json",
        "live probe: `curl /__agf/renderer-inspect` returns non-empty bucket + handle data on kaboom-crew"
      ],
      "epic": "AGF-ENGINE-OBSERVABILITY"
    },
    {
      "id": "AGF-WEBGPU-CHUNK-SPLIT",
      "title": "Investigate why Vite manualChunks doesn't split three.webgpu",
      "status": "pending",
      "summary": "The S70 WEBGPU-lazy-import story split `three/webgpu` into a dynamic import + manualChunks rule for `three-webgpu-*`. The split never produces a separate chunk in practice — Vite folds the WebGPU code straight back into the main `three-*` chunk (~520 KB). Investigate why (dynamic import shape, module specifier, manualChunks ordering) and ship a fix that actually code-splits, then tighten the budget back from 560 KB toward the original 340 KB.",
      "verification": [
        "build produces a `three-webgpu-*` chunk in `dist/assets/` for kaboom-crew (which doesn't use WebGPU)",
        "scripts/check-bundle-size.mjs three- budget lowered to ≤ 400 KB and passes"
      ],
      "epic": "AGF-ENGINE-OBSERVABILITY"
    }
  ]
}
