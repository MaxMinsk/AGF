{
  "agfFormatVersion": 1,
  "id": "S088",
  "title": "Render-pool warmup + unification + Kaboom Crew polish r3",
  "status": "active",
  "startedAt": "2026-05-20",
  "stories": [
    {
      "id": "AGF-POOL-INVENTORY-API",
      "title": "runtime.renderer.pools() lists every render pool's live + peak",
      "status": "implemented",
      "summary": "Surface a programmatic accessor on the renderer so an agent can ask which pools exist + how loaded they are. Mirrors AssetRegistry.inventory() shape. The adapter (`ThreeRenderAdapter`) already owns three RenderPoolRegistry instances (instanced, batched, particle); add a `pools(): ReadonlyArray<{ name; live; peak }>` method on the adapter and forward via `ThreeRenderer.pools()`. Includes peak-tracking on RenderPoolRegistry so the value survives release.",
      "verification": [
        "runtime.renderer.pools() returns ReadonlyArray<{ name: 'instanced'|'batched'|'particle'; live: number; peak: number }>",
        "RenderPoolRegistry gains peak() + reset(); acquire bumps peak; release does NOT decrease it; reset() drops counter+peak",
        "unit test exercises acquire/release/peak retention + reset"
      ],
      "epic": "AGF-ENGINE-OBSERVABILITY"
    },
    {
      "id": "AGF-POOL-INVENTORY-PROBE",
      "title": "GET /__agf/pool-inventory exposes the same shape over the dev bridge",
      "status": "implemented",
      "summary": "Mirror the S86 asset-inventory probe: dev bridge route `/__agf/pool-inventory` returns JSON identical to runtime.renderer.pools(). Lets the live-debug Playwright skill grab pool state in a single curl. Documented in docs/agent-probes.md.",
      "verification": [
        "GET /__agf/pool-inventory returns ReadonlyArray<{ name, live, peak }>",
        "page-bridge.ts forwards window.__agf.poolInventory(); agf-dev-bridge maps /pool-inventory → pool-inventory channel",
        "docs/agent-probes.md adds a /__agf/pool-inventory section beside asset/invalidate"
      ],
      "epic": "AGF-ENGINE-OBSERVABILITY"
    },
    {
      "id": "AGF-PARTICLE-PREWARM-SYSTEM",
      "title": "ParticleEmitterSystem accepts `preWarmPresets` for engine-level warmup",
      "status": "pending",
      "summary": "S85 AGF-POOL-WARMUP-PARTICLES shipped as a project-local hack (kaboom-crew spawns a hidden offscreen ParticleEmitter on attachUi to force shader compile). Promote it: the engine ParticleEmitterSystem factory accepts `preWarmPresets?: ReadonlyArray<string>` and on the first frame creates + destroys a single tiny emitter per requested preset, so the shader compile + GPU buffer upload happens before any gameplay emit. No more project-local warmup entities; kaboom-crew passes `[\"spark\", \"glow\"]` instead.",
      "verification": [
        "unit test: preWarmPresets=['spark'] → after first frameUpdate the pool registry's `peak` for the spark preset is >= 1 and `live` is back to 0",
        "live probe: first KABOOM-BLAST-PARTICLES burst no longer logs an AGF_FRAME_SPIKE",
        "kaboom-crew bootstrap drops the kaboom.warmup-particles entity creation and instead passes preWarmPresets to the system factory"
      ],
      "epic": "M19"
    },
    {
      "id": "AGF-POOL-DOCTOR-SECTION",
      "title": "engine doctor surfaces a Pools: section",
      "status": "pending",
      "summary": "Read the project's render-pool inventory at doctor-run time and print a 'Pools:' section: per pool, `name  live/capacity (peak N)`. Recommendation when a particle preset has `peak === 0` after >1 s of runtime (warmup ran but the project never emitted that preset — dead preset). Doctor unit test confirms the section renders for a fixture inventory.",
      "verification": [
        "DoctorReport.pools populated from runtime.pools.inventory() snapshot taken via the doctor probe path",
        "format pass prints a 'Pools:' section",
        "recommendation when preWarm preset has peak=0 after 1 s of runtime"
      ],
      "epic": "AGF-ENGINE-OBSERVABILITY"
    },
    {
      "id": "AGF-POOL-INVENTORY-TEST",
      "title": "Unit test for RenderPoolRegistry peak + reset tracking",
      "status": "implemented",
      "summary": "RenderPoolRegistry ships acquire/release but no test covers the peak-tracking invariant introduced by AGF-POOL-INVENTORY-API. Add: acquire bumps peak; release does NOT decrease peak; reset() drops to 0; drain() does NOT touch peak.",
      "verification": [
        "+4 unit tests under 'S88 AGF-POOL-INVENTORY-API peak tracking' describe block; existing RenderPoolRegistry suite stays green"
      ],
      "epic": "AGF-ENGINE-OBSERVABILITY"
    },
    {
      "id": "KABOOM-DROP-LOCAL-WARMUP",
      "title": "kaboom-crew drops the project-local warmup entity",
      "status": "pending",
      "summary": "Once AGF-PARTICLE-PREWARM-SYSTEM lands the project-local kaboom.warmup-particles entity is redundant. Delete the entity creation + the runtime.applyCommands hack from attachUi; kaboom-crew now relies entirely on the engine-level preWarmPresets option. Smaller bootstrap.ts; one less moving part.",
      "verification": [
        "examples/kaboom-crew/bootstrap.ts no longer contains the kaboom.warmup-particles entity.create command",
        "smoke playtest: first BlastEvent shows no spike"
      ],
      "epic": "KABOOM-CREW-MVP-1"
    },
    {
      "id": "KABOOM-WIN-CHIME",
      "title": "Short procedural chime on match win/lost/draw",
      "status": "pending",
      "summary": "When matchPhase flips from in-progress to won/lost/draw, audio-binding-system fires a new `match-end` AudioEventKind. audio-fx.ts adds three short procedural chimes (triumph triad / descending minor / neutral perfect-fifth). Inherits the existing volume dial. Defaults to one play per match resolution.",
      "verification": [
        "audio-binding-system emits 'match-end' exactly once per matchPhase transition out of in-progress",
        "audio-fx.ts plays a different chord for won / lost / draw",
        "+1 unit test in audio-binding-system.test.ts covers the transition edge"
      ],
      "epic": "KABOOM-CREW-MVP-1"
    },
    {
      "id": "KABOOM-BOT-DANGER-AVOID",
      "title": "Bot pathfinding penalises cells covered by live blasts + about-to-explode bombs",
      "status": "pending",
      "summary": "Today's BotAISystem treats BlastTile cells as walkable, so the bot regularly suicides into its own blast on the wide map. Extend the cost function in agent-goto-system (or a small wrapper inside bot-ai-system) so cells containing a BlastTile OR a Bomb with fuseRemaining < 0.6 are treated as high-cost (e.g. cost 10 vs. 1). Pure cost bump — keeps the existing BFS; no new components.",
      "verification": [
        "unit test: bot starting next to a live BlastTile picks a path that detours around it",
        "unit test: bot starting next to a fuseRemaining=0.3 bomb prefers the longer non-explosive path",
        "live probe: 60 s of bot-vs-bot at hard difficulty shows the bot suicide rate drop"
      ],
      "epic": "KABOOM-CREW-MVP-1"
    },
    {
      "id": "AGF-LOG-LIFECYCLE-SCHEDULER",
      "title": "scheduler.register emits an info-level AGF_SCHEDULER_REGISTER diagnostic",
      "status": "pending",
      "summary": "Engine lifecycle traces follow-up from S082 — partially shipped via AGF_SCENE_LOAD_APPLIED + AGF_ENTITY_RECREATED. Closes the loop by making scheduler.register emit `info` AGF_SCHEDULER_REGISTER { systemName, phase, profile } so a snapshot reconstructs the boot-time system list without inspecting the scheduler object. No-ops in production via the diagnostics severity filter (info-level diagnostics already dropped under the default warning gate).",
      "verification": [
        "Scheduler.register pipes through diagnostics when a bus is supplied; emission count == registered systems",
        "+1 unit test exercises register/unregister + asserts the diagnostic ledger",
        "boot-noise budget regression check stays green (info-level diagnostics filtered out)"
      ],
      "epic": "AGF-ENGINE-OBSERVABILITY"
    }
  ]
}
