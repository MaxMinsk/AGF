{
  "name": "agents-game-framework",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "engine:check": "tsx engine/tools/cli.ts check",
    "engine:inspect": "tsx engine/tools/cli.ts inspect",
    "backend:node": "tsx examples/backends/node-world-server/src/index.ts",
    "backend:node:serve": "tsx examples/backends/node-world-server/src/index.ts --serve",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "playwright test",
    "playtest:watch": "node scripts/watch-playtests.mjs",
    "preflight": "npm run engine:check -- examples/hello-3d && npm run typecheck && npm run test && npm run build && npm run test:e2e"
  },
  "engines": {
    "node": ">=20.19.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "@types/node": "^25.7.0",
    "@types/ws": "^8.18.1",
    "tsx": "^4.21.0",
    "typescript": "^6.0.3",
    "vite": "^8.0.12",
    "vitest": "^4.1.6",
    "ws": "^8.20.1"
  },
  "dependencies": {
    "@types/three": "^0.184.1",
    "ajv": "^8.20.0",
    "three": "^0.184.0"
  }
}
