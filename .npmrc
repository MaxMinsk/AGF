; Project-local override so the public npm registry is used regardless of
; the developer's machine-level ~/.npmrc (which may point at a private
; artifactory). Keeps package-lock.json's `resolved` URLs portable to CI
; and to any contributor without that artifactory's credentials.
registry=https://registry.npmjs.org/
