---
description: Work on asset sources, metadata, import, storage, loading or optimization
argument-hint: [asset task or question]
---

Use `ASSETS_SOURCES.md`, `BACKLOG.md`, `docs/STRUCTURE.md`, and `docs/research/threejs-best-practices.md` to answer or implement `$ARGUMENTS`.

Keep asset work aligned with:

- Meshy + Blender + CC0 libraries + procedural materials first;
- English filenames, ids, manifests and diagnostics;
- source assets separate from runtime exports;
- `assets/_sources/asset-sources.json` metadata;
- `glb` as the preferred runtime 3D format;
- primitive-first Beacon World assets before polished art;
- no untracked licenses or anonymous generated files.

If implementation is requested, update validation or docs so future agents can check asset references.

