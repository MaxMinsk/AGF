---
description: Review project docs for consistency with current rules
argument-hint: [optional topic]
---

Review documentation consistency for `$ARGUMENTS`.

Focus on:

- English-only repo rule;
- TypeScript client plus backend-agnostic persistent-world contracts;
- backend-agnostic contracts, with C# only as a reference backend path;
- pragmatic ECS and command pipeline;
- persistent shared-world sample game direction;
- repository root is the public engine repo and examples are nested projects;
- backlog split across HIGH_LEVEL_BACKLOG.md, BACKLOG.md and BACKLOG_ARCHIVE.md;
- no stale match/session wording where world persistence is intended;
- OpenGame lessons are captured as AGF-specific reliability work, not one-prompt generation goals;
- debug protocol, template policy and quality axes docs are linked when relevant;
- links between README, CLAUDE.md, AGENTS.md, BACKLOG.md and docs.

Suggest minimal edits. If edits are requested, keep them concise.
